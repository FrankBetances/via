<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Audio: micrófono (sonómetro, análisis de voz, T.A.R.) y sesión de
         audio para la síntesis de tonos de las audiometrías. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Vibración háptica: refuerzo de la recompensa en la audiometría
         condicionada infantil. -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- BLE: pulsioxímetro de la exploración de disfagia (react-native-ble-plx). -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />

    <!-- Ubicación: en Android ≤ 11 el escaneo BLE la exigía a nivel de sistema;
         desde Android 12 no hace falta porque BLUETOOTH_SCAN va marcado
         `neverForLocation`. De ahí el maxSdkVersion=30.

         Hay DOS fuentes de estos permisos aguas abajo, y cada una necesita su
         propia directiva porque el manifest merger indexa los nodos por
         (nombre de elemento, android:name):

         1. react-native-ble-plx los declara como <uses-permission> sin
            maxSdkVersion → `tools:node="replace"` hace que gane la declaración
            de la app.

         2. rxandroidble 1.17.2 (dependencia transitiva de ble-plx) los declara
            como <uses-permission-sdk-23>, que para el merger es un elemento
            DISTINTO: el `replace` de arriba ni lo mira, y el manifiesto
            fusionado acababa con las dos formas del mismo permiso, una con
            maxSdkVersion=30 y otra sin él. Play Console lo lee como una
            declaración duplicada con maxSdkVersion distinto y rechaza el
            bundle. `tools:node="remove"` es lo único que las quita.

         Quitarlas no cambia el comportamiento: minSdkVersion es 24, y a partir
         de API 23 <uses-permission> y <uses-permission-sdk-23> son idénticos
         (permiso peligroso, se pide en tiempo de ejecución). La distinción solo
         importaba en API < 23, que la app no soporta. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:maxSdkVersion="30" tools:node="replace" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" android:maxSdkVersion="30" tools:node="replace" />
    <uses-permission-sdk-23 android:name="android.permission.ACCESS_FINE_LOCATION" tools:node="remove" />
    <uses-permission-sdk-23 android:name="android.permission.ACCESS_COARSE_LOCATION" tools:node="remove" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
